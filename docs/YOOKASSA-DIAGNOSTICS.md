# Диагностика YooKassa Payment Service

## Проблема: invalid_credentials (401)

Если вы получаете ошибку `invalid_credentials` от YooKassa API, это означает, что credentials (shopId или secretKey) неверны или отсутствуют.

## Быстрая диагностика

### 1. Health Check Endpoint

Самый простой способ проверить статус YooKassa credentials:

```bash
curl http://localhost:8888/api/yookassa-health
```

Или в браузере откройте: `http://localhost:8888/api/yookassa-health`

Ответ будет содержать:

- `env.hasShopId` - есть ли YOOKASSA_SHOP_ID в env
- `env.hasSecret` - есть ли YOOKASSA_SECRET_KEY в env
- `yookassaTest.success` - успешен ли тестовый запрос к YooKassa API

### 2. Диагностический режим create-payment

Можно использовать диагностический режим без создания реального платежа:

```bash
curl -X POST http://localhost:8888/api/create-payment \
  -H "Content-Type: application/json" \
  -d '{"diagnose": true}'
```

## Локальная разработка

### Настройка переменных окружения

1. **Создайте `.env` файл в корне проекта** (если его нет):

```bash
YOOKASSA_SHOP_ID=ваш_shop_id
YOOKASSA_SECRET_KEY=ваш_secret_key
DATABASE_URL=postgresql://...
```

2. **Запустите Netlify Dev**:

```bash
netlify dev
```

Netlify Dev **автоматически** читает `.env` файл из корня проекта. Дополнительные опции не нужны.

### Проверка что env загружены

После запуска `netlify dev`, при каждом запросе к `/api/create-payment` в логах терминала вы увидите:

```
🔍 ENV check: {
  hasShopId: true,
  hasSecret: true,
  hasDb: true,
  ...
}
```

Если `hasShopId: false` или `hasSecret: false` - значит `.env` не читается или переменные пустые.

## Production (Netlify)

В production переменные окружения настраиваются через Netlify Dashboard:

1. Откройте: https://app.netlify.com/sites/YOUR-SITE/settings/env
2. Убедитесь, что установлены:
   - `YOOKASSA_SHOP_ID`
   - `YOOKASSA_SECRET_KEY`
   - `DATABASE_URL`

## Типичные проблемы

### Проблема: "Payment service not configured"

**Причина**: YOOKASSA_SHOP_ID или YOOKASSA_SECRET_KEY пустые или не заданы.

**Решение**:

1. Проверьте `.env` файл (локально) или Netlify Dashboard (production)
2. Убедитесь, что переменные не содержат лишних пробелов
3. Перезапустите `netlify dev` после изменения `.env`

### Проблема: "invalid_credentials" от YooKassa

**Причина**: Неверные shopId или secretKey.

**Решение**:

1. Проверьте credentials в YooKassa Dashboard
2. Убедитесь, что используете правильные значения (не тестовые, если нужны production)
3. Проверьте, что нет лишних пробелов: используйте `.trim()` (уже добавлено в код)

### Проблема: env пустые в функции, но есть в терминале

**Причина**: `netlify dev` был запущен до создания/обновления `.env` файла.

**Решение**:

1. Остановите `netlify dev` (Ctrl+C)
2. Убедитесь, что `.env` файл существует и содержит нужные переменные
3. Запустите `netlify dev` заново

## Логирование

Все логи credentials безопасны - полный secretKey никогда не логируется. В логах вы увидите только:

- `secretKeyPrefix` - первые 6 символов + `***`
- `secretKeyLength` - длина ключа
- `shopId` - полный shopId (не секретный)

## Проверка через curl

Для проверки credentials напрямую через curl:

```bash
# Загрузите переменные окружения
set -a
source .env
set +a

# Проверьте что переменные загружены
echo "Shop ID: $YOOKASSA_SHOP_ID"
echo "Secret length: ${#YOOKASSA_SECRET_KEY}"

# Тестовый запрос к YooKassa
curl -u "$YOOKASSA_SHOP_ID:$YOOKASSA_SECRET_KEY" \
  https://api.yookassa.ru/v3/payments?limit=1 \
  -H "Content-Type: application/json"
```

Если curl возвращает 200 - credentials корректны. Если 401 - проверьте значения в YooKassa Dashboard.
