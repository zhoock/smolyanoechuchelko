# 🚀 Быстрый запуск локальной разработки

## Шаг 1: Загрузить переменные окружения

**Вариант 1 (рекомендуется): Использовать `--live` флаг**

Команда `npm run dev` автоматически использует `netlify dev --live`, который загружает production переменные из Netlify.

**Вариант 2: Создать `.env` файл вручную**

Если переменные скрыты в Netlify Dashboard, создайте `.env` файл вручную:

```bash
# Создайте файл .env в корне проекта:
DATABASE_URL=postgresql://user:password@host:port/database
ENCRYPTION_KEY=ваш-ключ
JWT_SECRET=ваш-секрет
JWT_EXPIRES_IN=7d
```

**Вариант 3: Установить переменные для dev контекста в Netlify Dashboard**

1. Откройте: https://app.netlify.com/sites/YOUR-SITE/settings/env
2. Для каждой переменной нажмите "Options" → "Edit"
3. Добавьте значения для "Deploy Previews" и "Branch deploys"

## Шаг 2: Запустить dev сервер

```bash
npm run dev
# или
netlify dev
```

## Шаг 3: Открыть в браузере

```
http://localhost:8888
```

## ⚠️ Важно

- Используйте **`npm run dev`** (или `netlify dev`), а не просто `npm start`
- `npm start` запускает только webpack dev server **без** Netlify функций
- `npm run dev` запускает полный dev сервер с функциями на порту 8888

## 🔍 Проверка

1. Откройте `http://localhost:8888`
2. Перейдите на `/auth` и залогиньтесь (zhoock@zhoock.ru / 123123)
3. Перейдите на `/dashboard/albums` - должна открыться админка

## ❌ Проблемы

**Админка не открывается:**

1. Проверьте, что переменные окружения загружены: `echo $DATABASE_URL`
2. Убедитесь, что запущен `netlify dev`, а не просто `npm start`
3. Проверьте логи в терминале

**"DATABASE_URL is not set!":**

```bash
source scripts/load-netlify-env.sh
# затем
npm run dev
```
