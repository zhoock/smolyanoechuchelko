# Как получить строку подключения из Supabase

## 📍 Где найти Connection string

1. **В левом меню** нажмите на иконку **⚙️ (Settings)**
2. Выберите **"Database"** в списке
3. Прокрутите вниз до раздела **"Connection string"**
4. Вы увидите несколько вкладок:
   - **URI** (рекомендуется)
   - **Connection string**
   - **JDBC**
   - **Golang**
   - И другие

## 🔑 Какую строку использовать

### Вариант 1: URI (рекомендуется)

Выберите вкладку **"URI"** и скопируйте строку вида:

```
postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
```

**ВАЖНО:** Замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта.

### Вариант 2: Connection string (альтернатива)

Выберите вкладку **"Connection string"** и скопируйте строку вида:

```
postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
```

**ВАЖНО:** Замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта.

## 📋 Пример полной строки

После замены пароля строка будет выглядеть так:

```
postgresql://postgres:your_actual_password@db.abcdefghijklmnop.supabase.co:5432/postgres
```

Или для URI:

```
postgresql://postgres.abcdefghijklmnop:your_actual_password@aws-0-us-east-1.pooler.supabase.com:6543/postgres
```

## ✅ Что делать дальше

1. Скопируйте полную строку (с замененным паролем)
2. Вставьте в Netlify Dashboard:
   - Project configuration → Environment variables
   - DATABASE_URL → Production (и Local development)
3. Сохраните
4. Выполните миграцию

## 🔒 Безопасность

- **НЕ коммитьте** строку подключения в Git
- **НЕ делитесь** паролем публично
- Используйте переменные окружения в Netlify
