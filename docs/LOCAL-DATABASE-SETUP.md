# Локальная настройка DATABASE_URL для миграций

## 📋 Быстрая инструкция

Для выполнения миграций локально нужна та же строка подключения, что и в Netlify.

### Шаг 1: Получите DATABASE_URL из Netlify Dashboard

1. Откройте https://app.netlify.com
2. Войдите в свой аккаунт
3. Выберите ваш проект
4. Перейдите в **Project configuration** → **Environment variables**
5. Найдите переменную `DATABASE_URL`
6. **Скопируйте значение** (нажмите на значок глаза, чтобы показать значение)

### Шаг 2: Используйте строку подключения локально

**Способ 1: Экспорт в текущей сессии терминала**

```bash
# Замените ВАША_СТРОКА_ПОДКЛЮЧЕНИЯ на реальное значение из Netlify
export DATABASE_URL="ВАША_СТРОКА_ПОДКЛЮЧЕНИЯ"
export ENCRYPTION_KEY="ВАШ_КЛЮЧ_ШИФРОВАНИЯ"

# Выполните миграцию
npm run migrate

# Протестируйте подключение
npm run test-db
```

**Способ 2: Создайте локальный .env файл (не коммитится в Git)**

```bash
# Создайте файл .env (он уже в .gitignore)
cat > .env << 'EOF'
DATABASE_URL=ВАША_СТРОКА_ПОДКЛЮЧЕНИЯ
ENCRYPTION_KEY=ВАШ_КЛЮЧ_ШИФРОВАНИЯ
EOF

# Используйте dotenv или загрузите переменные вручную
source <(cat .env | sed 's/^/export /')
npm run migrate
```

**Способ 3: Автоматическая загрузка через Netlify CLI (рекомендуется)**

Самый простой способ — использовать скрипт для автоматической загрузки переменных:

```bash
# 1. Установите Netlify CLI (если еще не установлен)
npm install -g netlify-cli

# 2. Авторизуйтесь в Netlify
netlify login

# 3. Свяжите проект с Netlify
netlify link

# 4. Загрузите переменные окружения (автоматически)
source scripts/load-netlify-env.sh

# 5. Выполните миграцию
npm run migrate

# 6. Протестируйте подключение
npm run test-db
```

**Или используйте npm-скрипт:**

```bash
source <(npm run load-env 2>&1 | grep "^export")
npm run migrate
```

## ⚠️ Важно

- **Не коммитьте** `.env` файл в Git (он уже в `.gitignore`)
- **Не делитесь** строкой подключения публично
- Используйте **тестовую БД** для разработки, если возможно
- Для production используйте переменные из Netlify Dashboard

## 🔍 Пример строки подключения

**Supabase:**

```
postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres?sslmode=require
```

**Neon:**

```
postgresql://[USER]:[PASSWORD]@[ENDPOINT].neon.tech/[DB_NAME]?sslmode=require
```

**Локальная БД:**

```
postgresql://postgres:password@localhost:5432/payment_db
```

## ✅ После настройки

Проверьте подключение:

```bash
npm run test-db
```

Если всё правильно, вы увидите:

```
✅ Successfully connected to PostgreSQL database.
✅ Table "user_payment_settings" exists.
✅ Essential columns in "user_payment_settings" exist.
✅ Unique index on (user_id, provider) exists.
✅ Encryption and decryption functions work correctly.
✅ getDecryptedSecretKey works correctly with test data.
```
