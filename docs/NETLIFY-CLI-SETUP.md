# Установка и настройка Netlify CLI

## 📦 Установка Netlify CLI

### Способ 1: Через npm (рекомендуется)

```bash
npm install -g netlify-cli
```

### Способ 2: Через Homebrew (macOS)

```bash
brew install netlify-cli
```

### Способ 3: Через установщик

Скачайте установщик с официального сайта:

- https://cli.netlify.com/

## ✅ Проверка установки

```bash
netlify --version
```

Должна отобразиться версия CLI, например:

```
netlify-cli/17.38.1 darwin-arm64 node-v20.10.0
```

## 🔐 Первая настройка

### Шаг 1: Авторизация в Netlify

```bash
netlify login
```

Откроется браузер, где нужно:

1. Войти в свой аккаунт Netlify
2. Разрешить доступ для CLI

### Шаг 2: Свяжите проект с Netlify

Перейдите в директорию проекта:

```bash
cd /path/to/your-project
```

Свяжите проект:

```bash
netlify link
```

Вам будет предложено:

- **Выбрать существующий сайт** (Create & configure a new site / Link this directory to an existing site)
- Выберите "Link this directory to an existing site"
- Выберите ваш проект из списка

Или создайте новый сайт:

```bash
netlify init
```

## 📥 Загрузка переменных окружения

После настройки вы можете автоматически загружать переменные окружения:

```bash
source scripts/load-netlify-env.sh
```

Или проверьте список переменных:

```bash
netlify env:list
```

## 🆘 Решение проблем

### Проблема: "permission denied" при запуске netlify

**Решение:**

```bash
# Исправьте права доступа к конфигурационному файлу
sudo chown -R $(whoami) ~/.config/netlify
```

Или:

```bash
# Переустановите CLI через npm с правильными правами
npm uninstall -g netlify-cli
npm install -g netlify-cli
```

### Проблема: "Command not found: netlify"

**Решение:**

```bash
# Проверьте, что npm global bin в PATH
npm config get prefix

# Добавьте в PATH (для macOS/Linux)
export PATH="$(npm config get prefix)/bin:$PATH"

# Или используйте полный путь
/usr/local/bin/netlify --version
```

### Проблема: "Not authorized"

**Решение:**

```bash
# Повторная авторизация
netlify logout
netlify login
```

### Проблема: "Site not linked"

**Решение:**

```bash
# Свяжите проект заново
netlify link
```

Или удалите старую связь и создайте новую:

```bash
rm -rf .netlify
netlify link
```

## 📚 Полезные команды

```bash
# Проверить статус
netlify status

# Список переменных окружения
netlify env:list

# Добавить переменную окружения
netlify env:set DATABASE_URL "postgresql://..." --context production

# Удалить переменную окружения
netlify env:unset DATABASE_URL --context production

# Деплой на Netlify
netlify deploy

# Деплой в production
netlify deploy --prod
```

## 🔗 Полезные ссылки

- [Официальная документация Netlify CLI](https://cli.netlify.com/)
- [Netlify CLI на GitHub](https://github.com/netlify/cli)
