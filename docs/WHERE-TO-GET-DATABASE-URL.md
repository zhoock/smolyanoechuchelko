# Где взять DATABASE_URL?

## 🎯 Краткий ответ

`DATABASE_URL` — это строка подключения к вашей PostgreSQL базе данных. Вы можете создать бесплатную БД на одном из сервисов ниже.

## 🆓 Бесплатные варианты PostgreSQL

### 1. Supabase (рекомендуется)

**Создание:**

1. Откройте https://supabase.com/dashboard
2. Зарегистрируйтесь или войдите
3. Нажмите "New project"
4. Заполните:
   - Name: например, `payment-db`
   - Database Password: придумайте пароль (сохраните его!)
   - Region: выберите ближайший
5. Дождитесь создания проекта (~2 минуты)

**Получение DATABASE_URL:**

1. В проекте перейдите: **Settings** (⚙️) → **Database**
2. Найдите раздел **Connection string**
3. Выберите вкладку **URI**
4. Скопируйте строку вида:
   ```
   postgresql://postgres.[PROJECT-REF]:[YOUR-PASSWORD]@aws-0-[REGION].pooler.supabase.com:6543/postgres
   ```
5. **ВАЖНО:** Замените `[YOUR-PASSWORD]` на пароль, который вы указали при создании проекта
6. Или используйте **Connection string** (не URI):
   ```
   postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
   ```

### 2. Neon (альтернатива)

**Создание:**

1. Откройте https://console.neon.tech
2. Зарегистрируйтесь или войдите
3. Нажмите "Create project"
4. Заполните:
   - Project name: например, `payment-db`
   - Region: выберите ближайший
5. Дождитесь создания

**Получение DATABASE_URL:**

1. В проекте откройте **Dashboard**
2. Найдите **Connection string**
3. Скопируйте строку вида:
   ```
   postgresql://[USER]:[PASSWORD]@[ENDPOINT].neon.tech/[DB_NAME]?sslmode=require
   ```

### 3. Railway

**Создание:**

1. Откройте https://railway.app
2. Зарегистрируйтесь через GitHub
3. Нажмите "New Project"
4. Выберите "Provision PostgreSQL"
5. База создастся автоматически

**Получение DATABASE_URL:**

1. Откройте созданную PostgreSQL базу
2. Вкладка **Variables**
3. Найдите `DATABASE_URL` — это и есть ваша строка подключения

### 4. Render

**Создание:**

1. Откройте https://render.com
2. Зарегистрируйтесь
3. Нажмите "New +" → "PostgreSQL"
4. Заполните:
   - Name: например, `payment-db`
   - Database: оставьте по умолчанию
   - User: создастся автоматически
5. Нажмите "Create Database"

**Получение DATABASE_URL:**

1. В настройках базы найдите **Internal Database URL**
2. Скопируйте строку

## 📝 Формат DATABASE_URL

Строка подключения всегда выглядит так:

```
postgresql://[USERNAME]:[PASSWORD]@[HOST]:[PORT]/[DATABASE]?sslmode=require
```

**Примеры:**

- Supabase: `postgresql://postgres:your_password@db.xxxxx.supabase.co:5432/postgres?sslmode=require`
- Neon: `postgresql://user:pass@ep-xxxxx.us-east-2.aws.neon.tech/neondb?sslmode=require`
- Локальная: `postgresql://postgres:password@localhost:5432/payment_db`

## ✅ Что делать после получения DATABASE_URL

1. **Вставьте в Netlify Dashboard:**
   - Откройте: https://app.netlify.com/projects/smolyanoechuchelko
   - Project configuration → Environment variables
   - Найдите `DATABASE_URL`
   - В поле **Production** вставьте строку подключения
   - **ВАЖНО:** Также добавьте в поле **Local development (Netlify CLI)** (то же значение)
   - Нажмите Save

2. **Для локальной миграции:**
   ```bash
   export DATABASE_URL="полная_строка_подключения"
   export ENCRYPTION_KEY="ваш_ключ_из_netlify"
   npm run migrate
   ```

## 🆘 Нет базы данных?

Если у вас еще нет базы данных, создайте её на **Supabase** — это самый простой вариант:

- Бесплатный tier (500MB)
- Простая настройка
- Хорошая документация

**Ссылка:** https://supabase.com/dashboard → "New project"
