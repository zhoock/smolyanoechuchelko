# 🔍 Отладка локальной разработки

## Как проверить, что происходит

### 1. Откройте DevTools (Инструменты разработчика)

**Способ 1: Горячие клавиши**

- **Mac**: `Cmd + Option + I`
- **Windows/Linux**: `F12` или `Ctrl + Shift + I`

**Способ 2: Через меню**

- Правый клик на странице → "Inspect" / "Проверить элемент"
- Или меню браузера → "More tools" → "Developer tools"

### 2. Проверьте Console (Консоль)

1. Откройте вкладку **Console** в DevTools
2. Обновите страницу (`F5` или `Cmd+R`)
3. Ищите ошибки красного цвета

**Что искать:**

- ❌ `Failed to load resource` - файлы не загружаются
- ❌ `Uncaught Error` - ошибка в JavaScript
- ❌ `404` - файл не найден
- ❌ `500` - ошибка сервера

**Примеры ошибок:**

```
Failed to load resource: the server responded with a status of 404 (Not Found)
Uncaught ReferenceError: React is not defined
```

### 3. Проверьте Network (Сеть)

1. Откройте вкладку **Network** в DevTools
2. Обновите страницу (`F5` или `Cmd+R`)
3. Ищите файлы JavaScript

**Что проверять:**

**Колонка "Name" (Имя):**

- Должны быть файлы типа `main.js`, `scripts/main.js` или похожие `.js` файлы
- Если их нет → JavaScript не загружается

**Колонка "Status" (Статус):**

- ✅ `200` - файл загружен успешно
- ❌ `404` - файл не найден
- ❌ `500` - ошибка сервера
- ⏳ `(pending)` - запрос завис

**Колонка "Type" (Тип):**

- Должно быть `script` или `javascript` для JS файлов

**Колонка "Size" (Размер):**

- Если `(failed)` или пусто → файл не загрузился

### 4. Что делать, если файлы не загружаются

1. **Кликните на запрос с ошибкой** (красный или с ошибкой)
2. Откройте вкладку **Preview** или **Response**
3. Посмотрите, что там написано

**Возможные проблемы:**

**404 Not Found:**

- Файл не найден
- Проверьте путь к файлу в `webpack.config.js`
- Проверьте, что webpack dev server запущен

**500 Internal Server Error:**

- Ошибка на сервере
- Проверьте логи в терминале, где запущен `npm run dev`

**CORS Error:**

- Проблема с CORS
- Проверьте настройки прокси в `webpack.dev.js`

### 5. Проверьте терминал

Посмотрите на терминал, где запущен `npm run dev`:

**Что искать:**

- ✅ `webpack compiled successfully` - сборка успешна
- ❌ `Error` или `Failed` - ошибка сборки
- ⚠️ `Warning` - предупреждения (могут быть не критичны)

### 6. Типичные проблемы

**Проблема: Страница пустая, в Console нет ошибок**

- Проверьте Network tab - загружаются ли JS файлы?
- Проверьте, что `id="root"` есть в `index.html`

**Проблема: 404 для всех JS файлов**

- Webpack dev server не запущен или не работает
- Проверьте, что порт 8080 свободен
- Перезапустите `npm run dev`

**Проблема: CORS ошибки**

- Проблема с прокси
- Проверьте `webpack.dev.js` - прокси должен быть пустым при `netlify dev`

**Проблема: "Cannot GET /"**

- Проблема с `historyApiFallback`
- Проверьте `webpack.dev.js`

### 7. Скриншоты для отладки

Если проблема не решается, сделайте скриншоты:

1. **Console tab** - все ошибки
2. **Network tab** - список всех запросов (особенно JS файлы)
3. **Терминал** - где запущен `npm run dev` (первые 20-30 строк)

Это поможет быстро найти проблему!
